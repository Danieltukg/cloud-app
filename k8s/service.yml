apiVersion: v1
kind: Service
metadata:
  name: app-svc
  namespace: production
spec:
  selector:
    app: cloud-deploy
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
