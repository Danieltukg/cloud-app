all:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
  children:
    masters:
      hosts:
        k3s-master-0:
          ansible_host: # terraform output master_ips[0]
    workers:
      hosts:
        k3s-worker-0:
          ansible_host: # terraform output worker_ips[0]
        k3s-worker-1:
          ansible_host: # terraform output worker_ips[1]
